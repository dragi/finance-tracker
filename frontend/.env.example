# Base URL of the deployed API (see ApiUrl stack output)
VITE_API_URL=https://example.execute-api.us-east-1.amazonaws.com/prod

# Cognito user pool app client used for login (see UserPoolClientId stack output)
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_REGION=us-east-1
